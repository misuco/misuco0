// osc-interface.js

// OSC message logging to browser console

function logOSCMessage(message) {
    console.log('Received OSC Message:', message);
}

// Example usage
// logOSCMessage({'/example/address': 42});